name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
        
    - name: Inject EmailJS configuration securely
      run: |
        echo "🔑 Injecting EmailJS configuration directly into HTML..."
        
        # Verify secrets exist
        test -n "${{ secrets.EMAILJS_SERVICE_ID }}"  || (echo "❌ Missing EMAILJS_SERVICE_ID" && exit 1)
        test -n "${{ secrets.EMAILJS_TEMPLATE_ID }}" || (echo "❌ Missing EMAILJS_TEMPLATE_ID" && exit 1)
        test -n "${{ secrets.EMAILJS_PUBLIC_KEY }}"  || (echo "❌ Missing EMAILJS_PUBLIC_KEY"  && exit 1)

        # Create secure inline configuration in HTML
        # This prevents direct access to credentials via separate file
        
        # Create a temporary config file
        cat > email-config-inline.js << 'EOF'
    <script>
        // EmailJS configuration - injected securely during build
        window.EMAIL_CONFIG = {
          EMAILJS_SERVICE_ID: '${{ secrets.EMAILJS_SERVICE_ID }}',
          EMAILJS_TEMPLATE_ID: '${{ secrets.EMAILJS_TEMPLATE_ID }}',
          EMAILJS_PUBLIC_KEY: '${{ secrets.EMAILJS_PUBLIC_KEY }}'
        };
        console.log('🔑 EmailJS configuration loaded securely');
        window.EMAIL_CONFIG_LOADED = true;
        window.dispatchEvent(new Event('emailConfigLoaded'));
    </script>
        EOF
        
        # Use Python to safely inject the configuration
        python3 << 'PYTHON_EOF'
        import re
        
        # Read the HTML file
        with open('index.html', 'r') as f:
            html_content = f.read()
        
        # Read the config to inject
        with open('email-config-inline.js', 'r') as f:
            config_content = f.read()
        
        # Insert before closing </head> tag
        html_content = html_content.replace('</head>', config_content + '\n</head>')
        
        # Write back to file
        with open('index.html', 'w') as f:
            f.write(html_content)
        
        print("✅ EmailJS configuration injected successfully")
        PYTHON_EOF
        
        # Clean up temp file
        rm email-config-inline.js
        
        echo "✅ EmailJS configuration injected securely into HTML"
        echo "🔒 No separate file created - credentials not directly accessible"

    - name: Cache-bust scripts
      run: |
        echo "🔄 Adding cache-busting..."
        sed -i "s/__BUILD__/${{ github.run_number }}/g" index.html
        echo "✅ Cache-busting applied"
    
    - name: Prepare site for deployment
      run: |
        echo "📦 Preparing site for deployment..."
        
        # Create clean build directory
        mkdir -p _site
        
        # Copy all files to _site
        rsync -av --exclude='_site' --exclude='.git' --exclude='.github' --exclude='node_modules' . _site/
        
        # Verify critical files exist
        echo "🔍 Verifying build..."
        
        if [ ! -f "_site/index.html" ]; then
          echo "❌ index.html missing from build"
          exit 1
        fi
        
        # Check if EmailJS configuration is properly injected in HTML
        if grep -q "window.EMAIL_CONFIG" "_site/index.html"; then
          echo "✅ EmailJS configuration found in HTML"
        else
          echo "❌ EmailJS configuration missing from HTML"
          exit 1
        fi
        
        if [ ! -f "_site/config/email-config.js" ]; then
          echo "❌ email-config.js missing from build"  
          exit 1
        fi
        
        echo "✅ Build verification passed"
        echo "📁 Config files included:"
        find _site -name "*.js" | grep -E "(email|config)" | sort
        echo "🔒 EmailJS credentials securely embedded in HTML (not accessible as separate file)"

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: Verify deployment
      run: |
        echo "🚀 Deployment completed!"
        echo "🌐 Site URL: ${{ steps.deployment.outputs.page_url }}"
        
        # Wait a bit for deployment to propagate
        echo "⏳ Waiting for deployment to propagate..."
        sleep 15
        
        # Test key files
        echo "🧪 Testing deployed files..."
        
        if curl -f -s "https://portfolio.theguywiththedogs.dev/config/email-config.js" > /dev/null; then
          echo "✅ email-config.js is accessible"
        else
          echo "❌ email-config.js is NOT accessible"
        fi
        
        # Verify that email-secrets.js is NOT accessible (security check)
        if curl -f -s "https://portfolio.theguywiththedogs.dev/config/email-secrets.js" > /dev/null; then
          echo "⚠️ WARNING: email-secrets.js is accessible (security risk!)"
        else
          echo "✅ SECURE: email-secrets.js is not directly accessible"
        fi
        
        # Test that HTML contains configuration
        if curl -s "https://portfolio.theguywiththedogs.dev/" | grep -q "window.EMAIL_CONFIG"; then
          echo "✅ EmailJS configuration embedded in HTML"
        else
          echo "❌ EmailJS configuration missing from HTML"
        fi
        
        echo "🎉 Deployment verification complete!"